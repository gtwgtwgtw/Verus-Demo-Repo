[Require Synthesized] {
    // no preconditions
}

[Ensures Synthesized] {
    // x is not modified
    ensures(x@ == old(x@));
    // y ends up equal to the old y concatenated with exactly those elements of x divisible by 3, in order
    ensures(y@ == old(y@) + x@.filter(|v: u64| v % 3 == 0));
}
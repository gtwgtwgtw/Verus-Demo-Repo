[Require Synthesized]
0 <= N,
(N as usize) <= a.len(),
(N as usize) <= b.len()
[End Require]

[Ensures Synthesized]
forall |i: int| 0 <= i && i < N
    ==> a[i as usize] == b[i as usize] + 1 - i,
sum == (0..N).map(|i| b[i as usize] + 1 - i).sum()
[End Ensures]